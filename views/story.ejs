<body class="story">
    <main>
        <% singleStory.forEach(story => { %> 
            <% let storyTitle = story.title.split(' ').join('_').toLowerCase() %>           
            <h1><%- story.title %></h1>
            <% story.text.forEach(paragraph => {%>
            <p><%- paragraph.text %></p>
            <% }) %>
        <% }) %>
    </main>